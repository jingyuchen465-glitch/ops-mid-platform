# Bear MCP Single 面试文档

## 一、项目一句话介绍

`bear-mcp-single` 是一个面向 AI Agent 的企业 MCP 能力治理平台单体版。

它把企业内部已有的 HTTP API、数据库查询、文档资源、Prompt 模板和动态脚本能力，统一封装成 MCP 的 `Tools`、`Prompts`、`Resources` 和 `Skills`，让 Cursor、Codex、Claude 或自研 Agent 可以通过标准 `/mcp` 协议安全调用。

一句话概括：

> 这个项目解决的是“企业内部能力如何安全、标准化、可治理地接入 AI Agent”的问题。

## 二、项目为什么强

这个项目强的地方，不是简单写了几个 MCP Tool，而是做了一套完整的企业能力治理闭环：

```text
企业 API / 数据库 / 文档 / Prompt / 脚本
        ↓
MCP 标准封装
        ↓
Token 鉴权 + RBAC 权限 + Token 级选择
        ↓
Tools / Prompts / Resources / Skills
        ↓
AI Agent 动态发现和调用
        ↓
审计日志追踪
```

普通 MCP Demo 通常只是写几个 `@Tool`，例如 `hello`、`current_time`、`calculate`，只能证明会使用 MCP。

这个项目更进一步，重点解决企业落地时真正关心的问题：

- Agent 能看到哪些工具？
- 不同用户能不能看到不同工具？
- 同一个用户的不同 Token 能不能暴露不同能力？
- 新增 Tool 能不能不改代码、不发版？
- Tool 背后能不能组合多个企业 API？
- 动态脚本能不能只访问白名单 API？
- 数据库查询能不能只读、限行、可审计？
- Prompt、Resource、Skill 能不能也纳入权限体系？
- Agent 调用了什么能力，后续能不能追踪？

所以它不是一个工具调用 Demo，而是一个企业 AI Agent 能力中台的雏形。

## 三、技术栈

```text
Java 17
Spring Boot 3.3
Spring AI MCP Server WebMVC
MyBatis
MySQL
Groovy 动态脚本
OkHttp
HikariCP
Vue 管理后台
火山 TOS 对象存储
```

MCP 端点由 Spring AI 自动暴露：

```text
POST /mcp
```

核心配置在：

```yaml
spring:
  ai:
    mcp:
      server:
        protocol: STREAMABLE
```

## 四、整体架构

项目是单体 Spring Boot 工程，但内部按职责拆得比较清楚：

```text
gateway
  MCP 协议 Filter，处理鉴权、tools/list、tools/call、prompts、resources

core/auth
  Token 校验、用户角色权限构建

core/dynamic
  动态 Tool 查询、权限判断、执行入口

core/groovy
  Groovy 脚本执行、安全限制、runRequest/runSql 注入

core/request
  HTTP API 请求配置执行，支持参数模板、超时、限流

core/datasource
  外部数据源连接池和只读 SQL 查询

core/tools
  内置 MCP Tools 注册

core/prompt
  Prompt 模板和运行时权限控制

core/resource
  Resource 权限控制和对象存储读取

admin / share
  管理后台、创作空间、社区能力
```

核心链路：

```text
AI Client
  -> POST /mcp + Authorization: Bearer mcp_xxx
  -> ApiKeyAuthFilter
  -> TokenService
  -> McpUserContextHolder
  -> tools/list / tools/call / prompts/list / resources/read
  -> 内置 Tool 或动态 Groovy Tool
  -> runRequest / runSql
  -> mcp_audit_log
```

## 五、核心请求链路

### 1. Token 鉴权

MCP 客户端请求 `/mcp` 时携带：

```text
Authorization: Bearer mcp_xxx
```

`ApiKeyAuthFilter` 会拦截 `/mcp` 请求，提取 Bearer Token，交给 `TokenService` 校验。

`TokenService` 做几件事：

```text
1. 对明文 Token 计算 SHA-256
2. 查询 mcp_user_token.token_hash
3. 校验 Token 是否启用、是否过期
4. 查询 Token 归属用户
5. 查询用户角色
6. 过滤启用角色
7. 查询角色拥有的 Tool / Prompt / Resource 权限
8. 构建 TokenAuthInfo
```

最后 `ApiKeyAuthFilter` 把当前用户信息写入 `McpUserContextHolder`。

这样后面的 Filter、内置 Tool、动态 Tool 都可以拿到当前用户身份、tokenId、角色权限和客户端 IP。

面试表达：

> `/mcp` 不是我自己写的 Controller，而是 Spring AI MCP Server 自动注册的，所以我用 Servlet Filter 在 MCP Handler 之前做 Token 鉴权，并把用户上下文放进 ThreadLocal，后续 tools/list、tools/call、prompts、resources 都基于这个上下文做权限过滤。

### 2. tools/list

`tools/list` 是 MCP 工具发现入口。

Spring AI 原生只能列出 `@Tool` 注册的内置工具，但项目里的动态工具存在数据库里，所以需要改写 `tools/list` 响应。

流程：

```text
1. McpToolsListFilter 判断 JSON-RPC method 是否为 tools/list
2. 先放行给 Spring AI
3. Spring AI 返回原始内置工具列表
4. Filter 读取响应体
5. DynamicToolService 计算当前用户最终可见工具
6. 保留当前 Token 选择过的内置工具
7. 把数据库动态工具转换成 MCP Tool 格式追加进去
8. 返回改写后的 tools/list
```

最终可见工具计算规则：

```text
最终工具 = Token 选择的工具 ∩ 用户角色有权限的工具 ∩ 工具启用状态
```

面试表达：

> 我没有直接把所有工具都暴露给 Agent，而是在 `tools/list` 阶段做了二次治理。角色权限决定用户原则上能用哪些工具，Token 选择决定当前这把 Token 实际暴露哪些工具，两者取交集后才返回给客户端。

### 3. tools/call

`tools/call` 是 MCP 工具调用入口。

项目分两种工具：

```text
内置工具：Spring AI @Tool 方法执行
动态工具：McpToolsCallFilter 拦截后执行 Groovy 脚本
```

调用流程：

```text
1. McpToolsCallFilter 判断 method 是否为 tools/call
2. 解析 params.name 得到 toolName
3. 统一校验当前用户是否有角色权限
4. 校验当前 Token 是否选择了该工具
5. 如果是内置工具，放行给 Spring AI 执行，并记录审计
6. 如果是动态工具，调用 DynamicToolService.execute
7. DynamicToolService 查询 mcp_dynamic_tool
8. 构造 ScriptContext
9. GroovyScriptEngine 执行脚本
10. 返回 JSON-RPC result 或 error
11. 写 mcp_audit_log
```

面试表达：

> `tools/call` 阶段我做了严格模式校验，即使客户端绕过 `tools/list` 直接调用工具，也必须同时满足角色权限和 Token 选择，否则会直接拒绝并记录审计。

## 六、动态 Tool 是最大亮点

动态 Tool 的作用是把企业内部已有能力封装成 Agent 能发现、能理解、能调用的 MCP Tool。

普通内置工具需要写 Java 代码：

```java
@Tool(name = "query_order")
public Object queryOrder(...) {
    ...
}
```

新增工具需要：

```text
写代码 -> 编译 -> 发版 -> 重启
```

动态 Tool 不需要发版，它存在数据库里：

```text
mcp_dynamic_tool
  tool_name
  tool_description
  input_schema
  groovy_script
  linked_request_keys
  linked_data_source_ids
  is_enabled
  publish_status
```

配置完成后，Agent 调 `tools/list` 能看到它，调 `tools/call` 能执行它。

### 动态 Tool 能封装什么

只要后端能以受控方式调用，就可以封装成 Tool，例如：

```text
HTTP API
订单查询
退款查询
支付流水查询
日志查询
只读 SQL 查询
RAG 知识库检索
多个 API 的组合流程
第三方系统能力
简单数据加工逻辑
```

但不是“让 Agent 随便调用任何东西”，而是有边界：

```text
HTTP 必须走 runRequest，并且 key 必须在 linked_request_keys 白名单中
SQL 必须走 runSql，并且数据源必须在 linked_data_source_ids 白名单中
SQL 只允许 SELECT
Groovy 禁止危险类和危险方法
脚本有超时控制
所有调用写审计日志
```

### 动态 Tool 示例：封装订单查询 API

先配置 API：

```text
config_key = query_order_api
method = GET
url = http://order-service/orders/{{orderNo}}
```

再配置动态 Tool：

```text
tool_name = query_order
description = 根据订单号查询订单详情
input_schema = {"type":"object","properties":{"orderNo":{"type":"string"}}}
linked_request_keys = ["query_order_api"]
```

Groovy 脚本：

```groovy
def orderNo = params.orderNo
return runRequest.runRequest("query_order_api", [
    orderNo: orderNo
])
```

Agent 看到的是：

```text
query_order(orderNo)
```

它不需要知道真实 URL、Header、数据库、鉴权方式。

## 七、多个 API 组合流程怎么用

动态 Tool 不只能封装单个 API，也能编排多个 API。

比如“排查订单退款失败原因”，背后可能要查：

```text
订单详情 API
支付流水 API
退款记录 API
风控状态 API
```

可以创建一个动态 Tool：

```text
tool_name = diagnose_refund_failure
description = 根据订单号排查退款失败原因
input_schema = orderNo
linked_request_keys = [
  "query_order_detail",
  "query_payment_record",
  "query_refund_record",
  "query_risk_status"
]
```

Groovy 脚本：

```groovy
def orderNo = params.orderNo

def order = runRequest.runRequest("query_order_detail", [
    orderNo: orderNo
])

def payment = runRequest.runRequest("query_payment_record", [
    orderNo: orderNo
])

def refund = runRequest.runRequest("query_refund_record", [
    orderNo: orderNo
])

def risk = runRequest.runRequest("query_risk_status", [
    orderNo: orderNo
])

def conclusion = "需要人工结合返回字段进一步判断"

if (payment == null || payment.body == null || payment.body == "") {
    conclusion = "未查询到支付流水，可能订单未支付成功"
} else if (refund == null || refund.body == null || refund.body == "") {
    conclusion = "未查询到退款记录，可能尚未发起退款"
} else if (risk != null && risk.body != null && risk.body.contains("REJECT")) {
    conclusion = "风控接口显示退款被拦截"
}

return [
    orderNo: orderNo,
    order: order,
    payment: payment,
    refund: refund,
    risk: risk,
    conclusion: conclusion
]
```

Agent 只需要调用一个工具：

```text
diagnose_refund_failure(orderNo)
```

服务端内部完成多接口调用、结果汇总和初步判断。

面试表达：

> 动态 Tool 不只是 API 包装器，还可以做轻量业务编排。比如一个订单排障工具背后可以依次调用订单、支付、退款、风控多个 API，再把结果合并成一个结构化响应返回给 Agent。这样 Agent 不需要自己理解底层接口顺序，也避免漏调或错调。

## 八、runRequest 的作用

`runRequest` 是暴露给 Groovy 脚本的受控 HTTP API 调用入口。

它解决两个问题：

```text
1. 脚本不直接写 URL、Header、超时、鉴权细节
2. 脚本只能调用当前 Tool 绑定过的 request config
```

调用方式：

```groovy
runRequest.runRequest("demo_clock", [
    message: params.message
])
```

内部流程：

```text
1. 校验 demo_clock 是否在 linked_request_keys 中
2. RequestConfigService 查询 mcp_request_config
3. 合并 params_default 和运行时 params
4. 替换 URL / bodyTemplate 中的 {{变量}}
5. 检查限流
6. 用 OkHttp 发起 HTTP 请求
7. 返回 status + body
```

这相当于把企业 API 做成受控能力，不把真实接口细节暴露给 Agent。

## 九、runSql 的作用

`runSql` 是暴露给 Groovy 脚本的受控数据源查询入口。

它只能访问当前动态 Tool 绑定的数据源：

```text
linked_data_source_ids = [1]
```

脚本：

```groovy
return runSql.runSql("SELECT id, name FROM user LIMIT 10")
```

安全限制：

```text
SQL 不能为空
SQL 最大 10000 字符
不允许包含分号
必须以 SELECT 开头
最多返回 500 行
只能查绑定的数据源
```

面试表达：

> 数据库能力不是直接暴露给 Agent 的，而是通过动态 Tool 和数据源白名单间接暴露。脚本只能查绑定的数据源，并且执行器只允许 SELECT、限制行数和 SQL 长度，降低误操作风险。

## 十、Prompt、Resource、Skill 的作用

这几个能力可以这样理解：

```text
Tool     = 让 Agent 做事
Prompt   = 教 Agent 怎么想、怎么问、怎么输出
Resource = 给 Agent 提供可读取资料
Skill    = 给 Agent 安装一套可复用能力包
```

### Prompt

Prompt 是可复用提示词模板。

它适合沉淀：

```text
固定分析流程
标准输出格式
业务排障步骤
角色设定
回答规范
工具调用建议
```

例如订单排障 Prompt：

```text
你是订单系统排障助手。
请根据订单号 {{orderNo}} 和用户问题 {{question}} 分析。
排查顺序：
1. 查询订单详情
2. 查询支付流水
3. 查询退款记录
4. 根据证据输出结论和建议
```

Prompt 也走权限：

```text
mcp_role_prompt
mcp_user_prompt_selection
```

### Resource

Resource 是 Agent 可读取的企业资料。

它适合放：

```text
退款规则文档
订单状态码说明
接口字段说明
排障 SOP
研发规范
课程资料
指标口径文档
```

Resource 和 Tool 的区别：

```text
Tool 是执行动作
Resource 是读取资料
```

Resource 也走权限：

```text
mcp_role_resource
mcp_user_resource_selection
```

内容可以通过 TOS 对象存储读取。

### Skill

Skill 是能力包，比 Prompt 和 Resource 更大一层。

一个 Skill 可以包含：

```text
场景说明
使用流程
推荐 Prompt
推荐 Tool
相关 Resource
示例
注意事项
```

例如“退款排障 Skill”可以告诉 Agent：

```text
遇到退款失败问题时：
1. 先读取退款规则 Resource
2. 使用 refund_diagnosis_prompt
3. 调用 diagnose_refund_failure Tool
4. 按证据、原因、建议格式输出
```

面试表达：

> Tools 解决动作调用，Prompt 解决任务模板，Resource 解决知识上下文，Skill 解决一类场景的能力打包。这个项目的强点是把它们放进同一套 MCP 和权限体系里。

## 十一、RAG 能不能封装成 Tool

可以。

RAG 本质上也是一种企业能力，只要它能通过 HTTP API 或后端服务调用，就可以封装成 MCP Tool。

例如已有 RAG 检索接口：

```text
POST /api/rag/search
```

配置成 `mcp_request_config`：

```text
config_key = rag_search
method = POST
url = http://knowledge-service/api/rag/search
body_template = {"query":"{{query}}","topK":{{topK}}}
```

动态 Tool：

```text
tool_name = search_company_knowledge
description = 检索公司知识库，适合查询制度、产品文档、接口说明、排障手册
input_schema = query, topK
linked_request_keys = ["rag_search"]
```

Groovy 脚本：

```groovy
def query = params.query
def topK = params.topK ?: 5

return runRequest.runRequest("rag_search", [
    query: query,
    topK: topK
])
```

RAG Tool 和 Resource 的区别：

```text
Resource：读取一个确定文档
RAG Tool：根据 query 在大量文档中检索相关内容
```

面试表达：

> 可以把 RAG 封装成动态 Tool。RAG 检索接口先作为 request config 注册，再创建 `search_company_knowledge` 这样的动态 Tool，脚本通过 `runRequest` 调用 RAG API。这样 Agent 在回答问题前可以先调用知识库检索工具，拿到相关片段、来源和引用。

## 十二、审计日志

企业场景里，Agent 调用了内部能力，必须可追踪。

项目里内置工具、动态工具、Prompt、Resource 都会记录审计。

审计信息包括：

```text
用户 ID
用户名
工具名 / Prompt 名 / Resource URI
调用状态
耗时
请求参数摘要
响应摘要
错误信息
```

对应表：

```text
mcp_audit_log
```

面试表达：

> 审计日志让这个项目从“能调用”变成“可治理”。企业 AI Agent 不能只看调用成功，还要知道谁在什么时候通过哪把 Token 调了哪个能力、传了什么参数、返回了什么结果，方便排查问题和做安全追踪。

## 十三、管理后台和创作空间

项目不只是后端接口，还有 `admin-web` 和 `admin/share/studio` 相关能力。

后台能力包括：

```text
用户管理
角色管理
Token 管理
Token 工具选择
动态 Tool 管理
API 请求配置
数据源管理
Prompt 管理
Resource 管理
Skill 管理
审计日志查看
社区能力展示
```

这形成了一个闭环：

```text
创建 API / 数据源 / Prompt / Resource
        ↓
创建动态 Tool 或 Skill
        ↓
配置角色权限
        ↓
配置 Token 选择
        ↓
Agent 调用
        ↓
审计日志查看
```

面试表达：

> 我不是只做了 MCP 后端调用链路，还做了配置入口。这样业务能力可以通过后台被创建、选择、授权、调用和审计，形成一个可运营的平台闭环。

## 十四、数据库核心表

核心表可以按几组来讲。

### 用户和权限

```text
mcp_user
mcp_role
mcp_user_role
mcp_role_tool
mcp_role_prompt
mcp_role_resource
```

### Token 选择

```text
mcp_user_token
mcp_user_tool_selection
mcp_user_prompt_selection
mcp_user_resource_selection
```

### 能力配置

```text
mcp_dynamic_tool
mcp_request_config
mcp_data_source
mcp_prompt_template
mcp_resource
mcp_skill
```

### 审计和社区

```text
mcp_audit_log
mcp_community_like
```

权限核心公式：

```text
最终可用能力 = 角色权限 ∩ Token 选择 ∩ 能力启用/发布状态
```

## 十五、项目风险和改进

这个项目作为面试项目很完整，但也可以主动讲生产改进点。

### 1. 配置安全

当前 `application.yml` 中存在数据库地址、用户名和密码，课堂演示可以，但真实项目不能这样做。

改进：

```text
使用环境变量
使用 application-local.yml 并加入 .gitignore
使用配置中心或密钥管理服务
数据库不用 root 用户
```

### 2. 限流

当前 request config 限流是单机内存计数。

改进：

```text
使用 Redis 做分布式限流
按 userId / tokenId / toolName / configKey 多维度限流
增加熔断和降级
```

### 3. Groovy 沙箱

当前 Groovy 有字符串校验和 AST 限制，但不是绝对安全沙箱。

改进：

```text
更严格的 AST 白名单
独立进程或容器执行脚本
脚本 CPU / 内存限制
脚本审批和版本管理
禁用更多危险语法
```

### 4. 动态 Tool 版本管理

单体版主要是课堂版配置，生产可以增强：

```text
草稿版本
发布版本
历史版本
审批流
灰度发布
回滚
```

### 5. 测试和可观测性

改进：

```text
补充 tools/list 和 tools/call 集成测试
补充 Groovy 安全测试
补充权限绕过测试
增加 Prometheus 指标
增加调用链 traceId
```

主动讲这些不会减分，反而说明你知道教学项目和生产系统的差距。

## 十六、面试高频问题

### Q1：这个项目解决了什么问题？

它解决的是企业内部能力如何安全接入 AI Agent 的问题。

企业内部有很多 API、数据库、文档和 Prompt，但它们不是为 Agent 直接调用设计的。如果让 Agent 直接调 HTTP，会遇到接口分散、鉴权不统一、权限不好控制、审计缺失、新增能力要改 Agent 等问题。

所以我基于 MCP 做了一层标准化平台，把这些能力封装成 Tools、Prompts、Resources 和 Skills。Agent 通过 `tools/list` 动态发现能力，通过 `tools/call` 调用能力，平台统一负责 Token 鉴权、RBAC、Token 选择、白名单、审计和动态扩展。

### Q2：为什么不用普通 API 网关？

普通 API 网关面向系统调用，主要解决路由、鉴权、限流、负载均衡。

MCP 平台面向 AI Agent，除了调用 API，还要让 Agent 知道这个能力叫什么、干什么、什么时候用、参数是什么、返回什么。

MCP Tool 有 `name`、`description`、`inputSchema`，Agent 可以通过 `tools/list` 理解和选择工具。

所以：

```text
API 网关：系统访问 API
MCP 平台：AI 发现、理解、选择并安全调用企业能力
```

### Q3：动态 Tool 有什么用？

动态 Tool 的作用是把企业内部已有能力动态封装成 MCP Tool。

以前新增一个 Tool 要写 Java 代码、编译、发版。现在只需要在后台配置工具名、描述、参数 schema、Groovy 脚本，并绑定允许访问的 API 或数据源，就能让 Agent 在 `tools/list` 里发现它，在 `tools/call` 里调用它。

它不是让 Agent 随便访问任何东西，而是在白名单、权限和审计控制下，把 HTTP API、只读 SQL、RAG 检索或多个接口编排成标准 MCP Tool。

### Q4：动态 Tool 是不是可以封装任何东西？

准确说，只要后端能以受控方式调用，就可以封装。

例如：

```text
HTTP API
数据库 SELECT 查询
RAG 检索接口
日志查询接口
第三方系统接口
多个 API 组合流程
```

但它必须受平台约束：

```text
必须先配置 request config
必须绑定 linked_request_keys
必须绑定数据源才能 runSql
SQL 只能 SELECT
脚本有安全限制和超时
调用有审计日志
```

### Q5：多个 API 组合流程怎么实现？

一个动态 Tool 的 Groovy 脚本里，可以按顺序调用多个 `runRequest`。

例如订单退款排障工具，背后依次调用订单详情、支付流水、退款记录、风控状态，然后汇总成一个结构化结果返回给 Agent。

这样 Agent 只看到一个工具：

```text
diagnose_refund_failure(orderNo)
```

但服务端内部完成了多 API 编排。

### Q6：Prompt、Resource、Skill 分别有什么用？

```text
Tool     = 让 Agent 做事
Prompt   = 教 Agent 怎么想、怎么问、怎么输出
Resource = 给 Agent 提供资料
Skill    = 把一类场景的方法、工具、资料打包成能力包
```

例如退款排障：

```text
Tool：查询订单、支付、退款
Prompt：规定排障步骤和输出格式
Resource：退款规则、状态码文档
Skill：把退款排障流程整体打包
```

### Q7：RAG 能不能封装成 Tool？

可以。

RAG 本质上也是一种企业能力。如果已有知识库检索接口，就可以先配置成 `mcp_request_config`，再创建动态 Tool，比如 `search_company_knowledge`，脚本通过 `runRequest` 调 RAG API。

Resource 适合读取确定文档，RAG Tool 适合根据 query 在大量文档里搜索相关内容。

### Q8：怎么保证安全？

主要有几层：

```text
Token 只存 SHA-256，不存明文
用户、角色、Token 三层权限控制
tools/list 和 tools/call 都校验权限
runRequest 只能调用白名单 API
runSql 只能查绑定数据源
SQL 只允许 SELECT
Groovy 禁止危险类和危险方法
脚本线程池执行，有超时
所有调用写审计日志
```

### Q9：如果客户端绕过 tools/list 直接 tools/call 怎么办？

`McpToolsCallFilter` 会再次校验权限。

必须同时满足：

```text
当前用户角色有该工具权限
当前 Token 选择了该工具
工具本身启用
```

否则直接返回错误，并记录审计。

### Q10：这个项目还有哪些不足？

可以主动讲：

```text
配置里不应该出现明文数据库密码
限流现在是单机内存，生产要用 Redis
Groovy 沙箱不是绝对安全，生产可放到独立容器执行
动态 Tool 版本、审批、灰度、回滚还可以增强
测试覆盖和可观测性还可以继续补
```

这说明我知道教学版和生产级系统的差距。

## 十七、3 分钟项目介绍模板

面试时可以直接按下面这段讲：

> 我这个项目叫 `bear-mcp-single`，是一个面向 AI Agent 的企业 MCP 能力治理平台单体版。它解决的问题是：企业内部有很多 HTTP API、数据库、文档、Prompt 和脚本能力，但这些能力原本不是给 AI Agent 直接调用的。如果每个 Agent 都单独接 API，会出现鉴权不统一、权限不好控、审计缺失、能力新增要发版等问题。
>
> 所以我基于 Spring AI MCP Server 做了一层标准 MCP 平台。Spring AI 负责暴露 `/mcp` 协议端点，我在外层通过 Filter 增强了 Token 鉴权、`tools/list` 权限过滤、`tools/call` 动态工具执行、Prompts 和 Resources 扩展。
>
> 项目里权限分两层：角色权限决定用户原则上能使用哪些工具，Token 选择决定当前这把 Token 实际暴露哪些能力。最终 `tools/list` 返回的是角色权限和 Token 选择的交集，`tools/call` 阶段也会再次校验，避免客户端绕过工具列表直接调用。
>
> 最大亮点是动态 Tool。普通 MCP Tool 要写 Java 代码和发版，我这里把动态工具存在 `mcp_dynamic_tool` 表中，包括工具名、描述、input schema、Groovy 脚本、关联 API 和关联数据源。用户在后台配置后，Agent 就能通过 `tools/list` 发现这个工具，通过 `tools/call` 调用它。
>
> 动态脚本不是完全开放的，而是只能使用平台注入的 `runRequest` 和 `runSql`。`runRequest` 只能调用当前工具绑定过的 API 白名单，`runSql` 只能查询绑定的数据源，并且只允许 SELECT、限制行数和 SQL 长度。所有工具调用都会记录审计日志。
>
> 除了 Tools，我还做了 Prompts、Resources 和 Skills。Prompt 用来沉淀标准任务模板，Resource 用来给 Agent 读取企业文档，Skill 用来把一类场景的方法、工具和资料打包成能力包。这样项目就不是简单工具调用，而是一个企业能力接入 AI Agent 的完整治理闭环。

## 十八、30 秒精简版

> `bear-mcp-single` 是一个企业 MCP 能力治理平台单体版。它基于 Spring AI MCP Server 暴露 `/mcp`，在外层通过 Filter 实现 Token 鉴权、RBAC、Token 级工具选择、`tools/list` 动态过滤和 `tools/call` 动态执行。最大亮点是动态 Tool：企业 API、只读 SQL、RAG 检索或多个 API 编排都可以通过数据库配置和 Groovy 脚本封装成 MCP Tool，不需要发版。脚本只能调用白名单 API 和绑定数据源，所有调用都有审计。项目还把 Prompt、Resource、Skill 纳入同一套权限体系，形成企业内部能力安全接入 AI Agent 的完整闭环。
