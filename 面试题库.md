# Bear MCP Single 真实面试题库

这份题库按真实面试官追问口吻写，不写“为什么这个项目不是普通 Demo”这种自我包装式问题。回答尽量像现场口述，重点是讲清楚你做了什么、难点在哪、为什么这么设计、有什么风险和改进。

## 一、项目整体

### Q1：你这个项目主要做了什么？

我这个项目是一个基于 MCP 协议的企业能力接入平台。

简单说，就是把企业内部已有的 HTTP API、数据库查询、文档、Prompt、动态脚本能力，封装成 AI Agent 可以调用的 MCP Tools、Prompts、Resources 和 Skills。

Agent 不需要直接知道内部接口地址、数据库连接、鉴权方式，只需要通过 MCP 的 `tools/list` 发现能力，再通过 `tools/call` 调用能力。平台在中间负责 Token 鉴权、角色权限、Token 级能力选择、白名单控制和审计日志。

### Q2：你在这个项目里具体做了哪些核心功能？

我主要做的是 MCP 运行链路和能力治理这块。

核心包括：

```text
1. /mcp 请求的 Token 鉴权
2. 根据用户角色和 Token 选择过滤 tools/list
3. 拦截 tools/call，区分内置 Tool 和动态 Tool
4. 动态 Tool 的 Groovy 脚本执行
5. runRequest API 白名单调用
6. runSql 只读数据源查询
7. Prompt / Resource 的 MCP 协议适配
8. 调用审计日志
9. 管理后台里的 Token、工具、API、数据源配置
```

我不是只写了几个 `@Tool`，更重点的是把 MCP 工具调用放进一套权限和治理体系里。

### Q3：你这个项目的业务背景是什么？

背景是企业内部有很多系统能力，比如订单查询、支付查询、退款查询、日志查询、数据查询、规则文档等。

这些能力原来是给系统或者人用的，不是给 AI Agent 直接调用的。如果让 Agent 直接调 HTTP API，会有几个问题：

```text
接口分散
鉴权不统一
权限不好控制
调用过程没有审计
新增能力需要改 Agent 或改代码
Agent 不理解接口语义
```

所以我做了一层 MCP 平台，把这些能力标准化成 Tool、Prompt、Resource，让 Agent 用统一方式调用，同时平台负责权限和审计。

### Q4：这个项目你觉得最有技术含量的地方是什么？

我觉得主要有两块。

第一块是我没有只用 Spring AI 默认能力，而是用 Filter 拦截 `/mcp` 请求，对 MCP 协议做了增强。比如 `tools/list` 原本只会返回 Spring AI 注册的内置工具，我会在响应回来之后再按当前 Token 权限过滤，并把数据库里的动态 Tool 注入进去。`tools/call` 也会先经过我的 Filter，判断有没有权限、是内置 Tool 还是动态 Tool。

第二块是 Groovy 动态 Tool。普通 Tool 要写 Java 代码、发版，我这里把工具名、描述、参数 schema、Groovy 脚本、关联 API 都放到数据库里，后台配置后就能变成一个 MCP Tool。为了安全，我没有让脚本随便执行，而是只注入 `runRequest` 和 `runSql`，并且加了白名单、只读 SQL、超时和审计。

所以我觉得技术含量主要在：一是理解 MCP 请求链路并用 Filter 做协议增强，二是用 Groovy 把 Tool 做成可动态扩展但又受控的能力。

## 二、MCP 和 Spring AI

### Q5：MCP 是什么？你为什么要用 MCP？

MCP 是 Model Context Protocol，是 AI Client 和工具服务之间的标准协议。

我用 MCP 的原因是它把工具能力标准化了。Agent 可以通过 `tools/list` 获取工具名称、描述、参数 schema，再通过 `tools/call` 调用工具。

如果不用 MCP，让 Agent 直接调 HTTP，就会和业务接口强耦合。Agent 要知道 URL、Header、参数、返回结构。用了 MCP 以后，底层可以是 HTTP、SQL、RAG、脚本编排，但对 Agent 来说都是统一的 Tool。

### Q6：Spring AI 不是已经提供 MCP Server 了吗？你自己还做了什么？

Spring AI 主要帮我暴露标准 `/mcp` 协议端点，并且能把 `@Tool` 方法注册成 MCP Tool。

但它不会自动解决企业权限治理和动态工具问题。

我自己做的主要是增强层：

```text
ApiKeyAuthFilter：给 /mcp 加 Token 鉴权
McpToolsListFilter：改写 tools/list，只返回当前 Token 可用工具，并追加动态工具
McpToolsCallFilter：拦截 tools/call，做权限校验和动态工具执行
McpPromptsFilter：处理数据库里的动态 Prompt
McpResourcesFilter：处理数据库和对象存储里的 Resource
```

所以 Spring AI 提供协议基础，我在上面补了企业治理能力。

### Q7：`/mcp` 不是你写的 Controller，你怎么做鉴权？

我用 Servlet Filter 做。

因为 `/mcp` 是 Spring AI MCP Server 自动注册的，不适合在 Controller 层处理。

`ApiKeyAuthFilter` 会在请求进入 Spring AI 之前拦截：

```text
1. 判断请求路径是不是 /mcp
2. 从 Authorization: Bearer xxx 取 Token
3. 调 TokenService 校验 Token
4. 查询用户、角色、工具权限、Prompt 权限、Resource 权限
5. 构建 McpUserContext
6. 放到 ThreadLocal
```

后面所有 MCP Filter 和 Tool 执行都从 `McpUserContextHolder` 取当前用户上下文。

### Q8：你为什么用 Filter 改写 `tools/list`？

因为 Spring AI 原始 `tools/list` 只知道 `@Tool` 注册的内置工具，不知道数据库里的动态 Tool。

而且不同 Token 能看到的工具也不一样，所以不能直接返回 Spring AI 的原始结果。

我的处理方式是：

```text
1. 先让 Spring AI 生成原始 tools/list
2. Filter 拿到响应体
3. 根据当前用户和 tokenId 计算最终可见工具
4. 过滤内置工具
5. 追加动态工具
6. 写回响应
```

这样 MCP Client 看到的就是“当前 Token 真正可用的工具列表”。

### Q9：如果客户端直接调用 `tools/call`，不走 `tools/list`，会不会越权？

不会。

我在 `tools/call` 阶段也做了执行前校验。

`McpToolsCallFilter` 会检查：

```text
当前用户角色是否有这个工具权限
当前 Token 是否选择了这个工具
```

只有两者都满足，才允许继续执行。

所以 `tools/list` 是展示层过滤，`tools/call` 是执行层防线。即使客户端手写 JSON-RPC 请求直接调工具，也绕不过权限校验。

## 三、权限和 Token

### Q10：你的权限模型怎么设计的？

我设计成两层：

```text
用户 -> 角色 -> 角色能力权限
用户 -> Token -> Token 能力选择
```

角色权限表示这个用户原则上可以使用哪些能力，是权限上限。

Token 选择表示当前这把 Token 实际暴露哪些能力，是最小暴露集合。

最终可用能力是：

```text
角色权限 ∩ Token 选择 ∩ 能力启用状态
```

### Q11：为什么还要 Token 级选择？RBAC 不够吗？

只靠 RBAC 粒度太粗。

一个用户可能是管理员，有很多工具权限，但不同 Agent 不应该都拿到全部能力。

比如：

```text
Cursor 里的 Token 只开放研发工具
数据分析 Agent 的 Token 只开放查询工具
测试 Token 只开放 hello/current_time
```

Token 级选择能做到最小权限。即使某把 Token 泄露，影响范围也只限于这把 Token 选择过的能力。

### Q12：用户鉴权通过以后，你后面怎么做权限控制？

鉴权不只是判断能不能访问 `/mcp`，更重要的是构建后面 MCP 调用需要的用户上下文。

客户端请求 `/mcp` 时带 Bearer Token，`ApiKeyAuthFilter` 会先校验 Token。校验通过后，`TokenService` 会查出：

```text
当前用户是谁
当前 Token 是哪一把
用户有哪些角色
角色允许哪些 Tool / Prompt / Resource
```

然后这些信息会放到 `McpUserContextHolder` 里。

后面工具列表展示时，会用这个上下文过滤工具，只返回当前 Token 能看到的工具。

工具调用时，也会用这个上下文再次校验，判断当前用户角色有没有权限、当前 Token 有没有选择这个工具。

所以 Token 鉴权不是一个独立登录动作，它直接决定了 MCP 协议里当前客户端能发现什么、能调用什么。

### Q13：用户有角色权限，但当前 Token 没选择这个工具，能调用吗？

不能。

这个项目里工具调用是严格模式。

角色权限只是说明用户原则上有资格使用这个工具，但当前这把 Token 还必须显式选择它。

所以最终判断是：

```text
角色有权限
并且 Token 已选择
并且工具启用
```

少任何一个条件，`tools/list` 不会展示，直接 `tools/call` 也会被拒绝。

## 四、动态 Tool

### Q14：你说的动态 Tool 到底是什么？

动态 Tool 是存在数据库里的 MCP Tool 配置。

它不是 Java 代码里写死的 `@Tool`，而是由这些字段定义：

```text
tool_name
tool_description
input_schema
groovy_script
linked_request_keys
linked_data_source_ids
is_enabled
publish_status
```

Agent 调 `tools/list` 时，服务端把这些数据库配置转换成 MCP Tool 描述。

Agent 调 `tools/call` 时，服务端执行对应的 Groovy 脚本。

### Q15：为什么要做动态 Tool？

因为企业内部能力变化很快。

如果每新增一个查询接口、排障流程、数据查询能力，都要写 Java 代码、编译、发版，效率很低。

动态 Tool 可以把能力配置化：

```text
配置工具名
配置工具描述
配置参数 schema
配置 Groovy 脚本
绑定允许调用的 API 或数据源
```

这样不发版就能新增 MCP Tool。

### Q16：动态 Tool 和内置 Tool 怎么区分？

内置 Tool 是 Java 代码里的 `@Tool` 方法，由 Spring AI 注册和执行。

动态 Tool 是数据库里的配置，由 `McpToolsCallFilter` 拦截后交给 `DynamicToolService` 执行。

适用场景不同：

```text
稳定通用能力 -> 内置 Tool
变化快、需要配置、需要轻量编排的能力 -> 动态 Tool
```

### Q17：动态 Tool 是不是可以封装任何东西？

准确说，只要后端能以受控方式调用，就可以封装。

比如：

```text
HTTP API
只读 SQL
RAG 检索
日志查询
订单排障流程
多个 API 编排
第三方系统接口
```

但不是无边界地封装。项目里有几个限制：

```text
HTTP 必须走平台提供的受控 API 调用入口
只能调用当前 Tool 绑定过的 API
SQL 必须走平台提供的受控数据库查询入口
只能查当前 Tool 绑定过的数据源
SQL 只允许 SELECT
Groovy 有安全限制和超时
调用会写审计日志
```

### Q18：你怎么让 Agent 知道一个工具什么时候该用？

主要靠工具名、工具描述和参数 schema。

Agent 选工具不是靠人点按钮，而是根据这些元信息判断当前问题适不适合调用某个工具。

如果 description 写得模糊，Agent 可能选错工具。

比如：

```text
差：查询订单
好：根据订单号查询订单详情，适用于排查订单状态、支付状态、退款状态
```

描述越清楚，Agent 越容易在正确场景选择它。

### Q19：Agent 调工具时，怎么知道要传哪些参数？

通过 MCP Tool 的参数 schema。

动态 Tool 配置里会保存参数 schema，`tools/list` 返回给 Agent 后，Agent 就知道这个工具需要什么参数、参数类型是什么、哪些是必填。

例如：

```json
{
  "type": "object",
  "properties": {
    "orderNo": {
      "type": "string",
      "description": "订单号"
    }
  },
  "required": ["orderNo"]
}
```

Agent 看到后就知道调用这个工具要传 `orderNo`。

这也是 MCP Tool 和普通 HTTP 接口的区别：它对 AI 是可理解的。

## 五、内部 API 调用和多 API 编排

### Q20：动态 Tool 背后如果要调用公司内部 API，你是怎么做的？

我的做法不是让 Groovy 脚本直接写 URL 去发 HTTP 请求，而是先把公司内部 API 配置到平台里，再让动态 Tool 通过一个受控入口调用。

大概流程是：

```text
1. 先在后台配置一个内部 API，比如订单查询 API
2. 配置它的 URL、method、headers、body 模板、超时时间等
3. 创建动态 Tool 时，把这个 API 绑定到当前 Tool
4. Groovy 脚本里只能调用当前 Tool 已绑定的 API
5. 真正发 HTTP 请求由平台统一处理
```

代码实现上，我给 Groovy 脚本注入了一个 `runRequest` 对象：

```groovy
runRequest.runRequest("query_order_detail", params)
```

执行时会先检查 `query_order_detail` 是否在当前 Tool 的 API 白名单里，通过后才会真正调用。

这样脚本不用关心真实 URL 和 Header，也不能随便访问没绑定过的内部接口。

### Q21：公司内部 API 是怎么变成动态 Tool 可调用能力的？

我把内部 API 抽象成一份请求配置。

这份配置里保存：

```text
调用标识
请求方法
请求地址
请求头
请求体模板
默认参数
连接超时
读取超时
限流配置
是否启用
```

动态 Tool 不直接保存真实 HTTP 细节，而是绑定这些请求配置。

比如订单详情 API 可以配置成：

```text
GET http://order-service/orders/{{orderNo}}
```

动态 Tool 调用时传入 `orderNo`，平台会替换模板变量，然后发起 HTTP 请求。

这样做的好处是：API 调用方式统一收口到平台，脚本只负责业务编排。

### Q22：如果一个业务问题需要查多个接口，你怎么处理？

我会把它封装成一个组合型动态 Tool。

比如排查退款失败，背后可能要查：

```text
订单详情
支付流水
退款记录
风控状态
```

这些接口都先配置到平台，再绑定到同一个动态 Tool。Groovy 脚本里按业务顺序调用它们，然后把结果汇总返回。

比如排查退款失败：

```groovy
def orderNo = params.orderNo

def order = runRequest.runRequest("query_order_detail", [orderNo: orderNo])
def payment = runRequest.runRequest("query_payment_record", [orderNo: orderNo])
def refund = runRequest.runRequest("query_refund_record", [orderNo: orderNo])
def risk = runRequest.runRequest("query_risk_status", [orderNo: orderNo])

return [
  orderNo: orderNo,
  order: order,
  payment: payment,
  refund: refund,
  risk: risk
]
```

Agent 只看到一个工具：

```text
diagnose_refund_failure(orderNo)
```

但后端内部完成了多个 API 的编排和结果汇总。

### Q23：为什么不让 Agent 自己分别调多个 Tool？

不是不能，而是固定业务流程更适合封装成一个动态 Tool。

如果让 Agent 自己调多个工具，可能会：

```text
漏调
顺序错
参数错
中间结果理解错
返回结果碎片化
```

如果后端动态 Tool 已经把流程固定下来，Agent 只调一个高层工具，稳定性更好。

比如订单退款排障，固定就是订单、支付、退款、风控几个步骤，那就适合沉淀成一个组合型 Tool。

### Q24：如果多个 API 里有一个失败，你怎么处理？

要看这个 API 是不是关键依赖。

关键依赖失败，可以直接返回失败：

```groovy
if (!order) {
  return [success: false, message: "订单详情查询失败"]
}
```

非关键依赖失败，可以降级返回部分结果：

```groovy
return [
  order: order,
  payment: payment,
  refund: refund,
  warning: "风控接口失败，仅返回部分诊断"
]
```

这些异常处理策略可以写在动态 Tool 脚本里，而不是让 Agent 临时判断。

## 六、数据库查询和数据源

### Q25：动态 Tool 如果要查数据库，你是怎么做的？

我不是让 Agent 直接连数据库，而是在动态 Tool 脚本里提供一个受控的数据源查询入口。

实现上，我给 Groovy 脚本注入了 `runSql`，它允许动态 Tool 查询绑定的数据源，但只能 SELECT。

如果 Tool 只绑定了一个数据源，可以写：

```groovy
runSql.runSql("SELECT id, name FROM user LIMIT 10")
```

如果绑定多个数据源，需要显式传 datasourceId：

```groovy
runSql.runSql(1L, "SELECT id, name FROM user LIMIT 10")
```

### Q26：怎么防止 SQL 风险？

`ExternalDataSourceSqlExecutor` 做了限制：

```text
SQL 不能为空
SQL 最大 10000 字符
不允许分号
必须以 SELECT 开头
最多返回 500 行
只能查当前 Tool 绑定的数据源
```

生产上还可以进一步使用只读数据库账号，或者限制只能执行预设 SQL 模板。

### Q27：为什么不让 Agent 直接连数据库？

直接连数据库风险太高。

Agent 可能查到不该看的表，也可能误执行写操作，而且不好审计。

项目里通过 `runSql` 间接查询，能做到：

```text
数据源白名单
SELECT-only
限制行数
记录审计
Token 权限控制
```

这比直接暴露数据库安全得多。

## 七、Groovy 安全

### Q28：为什么用 Groovy？

Groovy 适合做轻量动态编排。

动态 Tool 通常需要做这些事情：

```text
读取 params
调用一个或多个 API
做简单 if 判断
组装返回结构
```

这些逻辑如果都写 Java，就要频繁发版。用 Groovy 可以把变化快的业务编排配置化。

### Q29：Groovy 脚本安全吗？

不能说绝对安全。

动态脚本天然有风险，所以项目做了 MVP 级别的防护：

```text
字符串级危险内容校验
AST 编译阶段限制
禁止 Runtime、ProcessBuilder、System、Thread、File 等危险类
禁止 exec、exit、getRuntime、forName、invoke 等危险方法
独立线程池执行
30 秒超时
不暴露裸 HTTP 客户端
只注入 runRequest 和 runSql
```

如果生产化，我会增加脚本审批、版本管理，并考虑把脚本放到独立容器执行。

### Q30：怎么防止脚本随便访问公司内网？

单体版没有给 Groovy 暴露裸 HTTP 客户端。

脚本只能通过 `runRequest` 调用已经登记在 `mcp_request_config` 里的 API。

而且 `runRequest` 会检查当前 Tool 的 `linked_request_keys`，不在白名单里的 API 不能调用。

生产上还可以给 request config 增加域名白名单、URL 审批、禁止访问内网敏感地址等策略。

### Q31：脚本死循环怎么办？

脚本不是直接在 Web 请求线程执行，而是提交到线程池里。

执行时用 `future.get(timeout)` 设置超时，默认 30 秒。

如果超过时间，就返回脚本执行超时。

生产环境还可以继续增加 CPU、内存限制，或者把脚本放到独立进程/容器里执行。

## 八、Prompt / Resource / Skill

### Q32：Prompt 在你项目里是干什么的？

Prompt 是可复用提示词模板。

Tool 是让 Agent 做动作，Prompt 是告诉 Agent 怎么组织任务、怎么输出。

比如订单排障 Prompt 可以规定：

```text
先查订单
再查支付
再查退款
最后按原因、证据、建议输出
```

这样业务经验可以沉淀成模板，不用每次都靠用户手写提示词。

### Q33：Prompt 怎么做权限控制？

和 Tool 类似，也是角色权限加 Token 选择。

```text
mcp_role_prompt：角色是否有 Prompt 权限
mcp_user_prompt_selection：当前 Token 是否选择这个 Prompt
```

`McpPromptsFilter` 处理 `prompts/list` 和 `prompts/get` 时，会通过 `PromptAccessService` 判断当前用户是否可访问。

### Q34：Resource 是干什么的？

Resource 是给 Agent 读取的资料或文档。

比如：

```text
退款规则
订单状态码
接口字段说明
排障 SOP
研发规范
指标口径
```

Tool 是动作，Resource 是上下文资料。

Agent 可以先读取 Resource，再调用 Tool，最后结合规则和实时数据回答。

### Q35：Resource 和 RAG 有什么区别？

Resource 是读取确定文档。

RAG Tool 是根据 query 在大量文档里检索相关内容。

比如：

```text
Resource：读取 refund_policy_doc
RAG Tool：搜索“退款失败原因”
```

文档路径明确时用 Resource；文档多、需要搜索时用 RAG Tool。

### Q36：Skill 是什么？

Skill 可以理解成能力包。

它把一个场景需要的方法、工具、Prompt、Resource 和示例打包起来。

比如退款排障 Skill 可以包含：

```text
排障流程
推荐 Prompt
推荐 Tool
相关 Resource
标准输出格式
示例
```

它不是直接执行动作，而是让 Agent 获得一套可复用的方法论。

### Q37：Tool、Prompt、Resource、Skill 怎么区分？

我会这样区分：

```text
Tool：让 Agent 做事
Prompt：教 Agent 怎么想、怎么输出
Resource：给 Agent 提供资料
Skill：把一类任务的方法、工具、资料打包成能力
```

比如退款排障：

```text
Tool 查订单、支付、退款
Prompt 规定排障步骤
Resource 提供退款规则
Skill 把整个退款排障能力打包
```

## 九、RAG

### Q38：RAG 能不能接到你这个平台？

可以。

RAG 本质上也是一种企业能力。只要它能通过 HTTP API 或服务方法调用，就可以封装成 MCP Tool。

最简单的方式是：

```text
1. 把 RAG 检索接口配置成 mcp_request_config
2. 创建 search_company_knowledge 动态 Tool
3. input_schema 定义 query、topK
4. Groovy 里通过 runRequest 调 rag_search
5. 返回文档片段、分数、来源
```

### Q39：RAG Tool 应该返回什么？

最好返回结构化结果。

比如：

```json
{
  "query": "退款失败原因",
  "hits": [
    {
      "title": "退款规则说明",
      "content": "订单未支付成功时不能发起退款...",
      "score": 0.89,
      "source": "refund_policy.md"
    }
  ]
}
```

更进一步可以返回答案摘要、引用来源和建议下一步调用的 Tool。

## 十、审计

### Q40：为什么要做审计日志？

企业内部 Agent 调用工具，必须能追踪。

要知道：

```text
谁调用的
调用哪个工具
传了什么参数
返回什么结果
成功还是失败
耗时多少
错误是什么
```

否则出了问题无法排查，也无法做安全审计。

### Q41：审计日志记录哪些内容？

主要记录：

```text
userId
userName
toolName
status
durationMs
requestParams
responseSummary
errorMessage
createTime
```

参数和响应一般记录摘要，不建议把敏感数据完整落库。

### Q42：动态 Tool 失败会不会记录？

会。

`DynamicToolService.execute` 在 finally 里记录审计。

不管成功、脚本异常、权限失败、API 调用失败，都会记录状态、耗时和错误信息。

## 十一、数据库设计

### Q43：你这个项目核心表有哪些？

我会按几组讲。

用户权限：

```text
mcp_user
mcp_role
mcp_user_role
mcp_role_tool
mcp_role_prompt
mcp_role_resource
```

Token 选择：

```text
mcp_user_token
mcp_user_tool_selection
mcp_user_prompt_selection
mcp_user_resource_selection
```

能力配置：

```text
mcp_dynamic_tool
mcp_request_config
mcp_data_source
mcp_prompt_template
mcp_resource
mcp_skill
```

审计：

```text
mcp_audit_log
```

### Q44：角色权限和 Token 选择为什么要拆开？

因为它们解决的问题不一样。

角色权限表示用户原则上有没有资格使用某个工具。

Token 选择表示当前这把 Token 实际暴露哪些工具。

最终可用工具是两者交集。

### Q45：动态 Tool 和底层 API 配置是什么关系？

底层 API 配置保存“接口怎么调用”，比如 URL、method、headers、body 模板、超时时间。

动态 Tool 保存“Agent 看到的工具怎么定义”，比如工具名、描述、参数 schema、Groovy 脚本，以及这个工具允许调用哪些 API。

一个动态 Tool 可以绑定多个 request config，然后在脚本里通过 `runRequest` 调用，实现多 API 编排。

## 十二、场景题

### Q46：如果让你做一个订单排障助手，你怎么设计？

我会这样做：

```text
1. 配置订单详情 API
2. 配置支付流水 API
3. 配置退款记录 API
4. 配置风控状态 API
5. 创建动态 Tool diagnose_order_problem
6. 绑定这些 API 到 linked_request_keys
7. Groovy 脚本按顺序调用并汇总结果
8. 创建订单排障 Prompt，规定输出格式
9. 创建订单状态码 Resource，提供规则说明
10. 创建 Skill，把这套流程打包
11. 给角色和 Token 授权
```

这样 Agent 只调用一个高层工具，就能完成标准排障流程。

### Q47：如果要接入公司知识库，你怎么做？

如果是固定文档，用 Resource。

如果是大量文档检索，用 RAG Tool。

RAG Tool 的实现方式是：

```text
1. 知识库检索接口配置成 mcp_request_config
2. 创建 search_company_knowledge 动态 Tool
3. input_schema 包含 query 和 topK
4. Groovy 通过 runRequest 调 rag_search
5. 返回 hits、score、source、content
```

### Q48：如果某个 Agent 只能查数据，不能调用写接口，你怎么控制？

我会从几层控制：

```text
角色层只给查询工具权限
Token 选择只选择查询工具
动态 Tool 只绑定查询 API
SQL 只允许 SELECT
数据库账号使用只读账号
所有调用记录审计
```

这样即使 Agent 想调用写能力，也看不到、调不了。

## 十三、安全和生产化

### Q49：这个项目最大的安全风险是什么？

我觉得主要有两个。

第一是配置安全。当前课堂版 `application.yml` 里有数据库连接信息，生产必须改成环境变量、配置中心或本地私有 profile，不能提交明文密码，更不能用 root 用户。

第二是 Groovy 动态脚本。虽然项目做了安全限制，但动态脚本很难说绝对安全，生产要加审批、版本管理、独立执行环境和更严格的资源限制。

### Q50：怎么防止 SSRF？

单体版没有给 Groovy 暴露裸 HTTP 客户端。

脚本只能通过 `runRequest` 调用已经配置好的 request config，并且必须在当前 Tool 的 `linked_request_keys` 白名单里。

生产上还可以增强：

```text
request config 审批
域名白名单
禁止访问内网敏感地址
禁止跳转
记录完整调用日志
```

### Q51：如果上线生产，你会优先改什么？

我会优先改：

```text
1. 移除明文数据库密码，改环境变量或配置中心
2. 数据库不用 root，改最小权限账号
3. 限流从单机内存改 Redis
4. Groovy 脚本加审批、版本、回滚
5. 脚本执行隔离到独立进程或容器
6. request config 增加域名白名单和审批
7. 补权限绕过、安全、集成测试
8. 增加监控指标和 traceId
```

### Q52：限流怎么做的？有什么问题？

单体版里的 request config 限流是内存计数器，按 configKey 和分钟窗口统计。

这个适合课堂演示，但生产有问题：

```text
多实例之间不共享
服务重启计数丢失
维度比较单一
```

生产应该改成 Redis 分布式限流，并且可以按 tokenId、userId、toolName、configKey 多维度限流。

### Q53：怎么提升性能？

可以做这些优化：

```text
缓存 Token 权限上下文
缓存 tools/list 结果
缓存动态 Tool 配置
缓存 request config
审计日志异步写入
脚本线程池隔离
HTTP 调用设置超时
SQL 强制限制返回行数
```

但权限缓存要注意失效，比如角色权限或 Token 选择变更后，要及时刷新。

## 十四、压迫式追问

### Q54：你这个是不是就是 CRUD 加几个 Filter？

不是。

CRUD 只是配置管理手段，真正核心在 MCP 运行时治理链路。

项目的关键点是：

```text
MCP 协议 Filter 扩展
tools/list 响应改写
tools/call 动态分流
RBAC + Token selection 权限模型
Groovy 动态脚本执行
runRequest / runSql 白名单
Prompt / Resource 协议实现
审计闭环
```

这些都是围绕 AI Agent 安全调用企业能力设计的，不是普通后台 CRUD。

### Q55：如果面试官说 Groovy 不安全，你怎么回答？

我会承认 Groovy 动态脚本确实有风险。

我这个版本做的是 MVP 级别防护：

```text
危险类和方法限制
AST 编译阶段检查
线程池隔离
超时控制
不暴露裸 HTTP
API 和数据源白名单
审计日志
```

如果生产化，我会加脚本审批、版本管理、独立容器执行和资源限制。

这样既不硬吹，也说明我知道风险和改进方向。

### Q56：如果 Spring AI 升级后响应格式变了，你的 tools/list 改写会不会受影响？

有可能。

因为我现在是通过 Filter 读取响应体并改写 JSON，如果 Spring AI 的响应结构变化，确实需要适配。

这个方案的好处是实现简单、能快速接入动态工具；风险是和响应格式有一定耦合。

生产上可以考虑更深度地接入 Spring AI 的 ToolCallbackProvider 或协议扩展点，减少响应改写带来的耦合。

### Q57：你怎么证明权限真的生效？

我会从几个场景验证：

```text
1. Token 没选工具，tools/list 不返回
2. Token 没选工具，直接 tools/call 被拒绝
3. 角色没权限，tools/list 不返回
4. 角色没权限，直接 tools/call 被拒绝
5. 动态 Tool 绑定 API A，脚本调用 API B 会失败
6. 动态 Tool 未绑定数据源，runSql 会失败
7. SQL 非 SELECT 会失败
8. 所有失败都记录审计
```

这些测试能覆盖展示层、执行层和脚本层的权限边界。

## 十五、简短背诵

### Q58：你用 1 分钟介绍一下这个项目。

`bear-mcp-single` 是一个面向 AI Agent 的企业 MCP 能力治理平台。它基于 Spring AI MCP Server 暴露 `/mcp`，我在外层通过 Filter 做了 Token 鉴权、`tools/list` 权限过滤、`tools/call` 动态执行，以及 Prompt、Resource 扩展。

项目最大亮点是动态 Tool。企业 API、只读 SQL、RAG 检索、多 API 编排，都可以通过数据库配置和 Groovy 脚本封装成 MCP Tool，不需要发版。脚本只能调用绑定过的 API 和数据源，SQL 只允许 SELECT，所有调用都会审计。

权限上做了 RBAC 和 Token 级选择。角色权限是上限，Token 选择是当前 Agent 实际暴露的能力，两者取交集，符合企业最小权限原则。

### Q59：如果只能讲一个亮点，你讲什么？

我讲动态 Tool。

因为它解决了企业 AI Agent 能力变化快的问题。以前新增 Tool 要写 Java 和发版，现在可以通过数据库配置工具名、描述、参数 schema、Groovy 脚本、关联 API 和数据源，让 Agent 动态发现和调用。

同时它不是裸执行，`runRequest` 和 `runSql` 都有白名单，脚本有安全限制和超时，调用有审计。

这就是这个项目从 Demo 变成企业治理平台的关键。

### Q60：这个项目如果被问有什么不足，你怎么说？

我会说这是单体课堂版，主线是完整的，但生产化还要补：

```text
配置安全
分布式限流
Groovy 脚本隔离
动态 Tool 版本和审批
更完整的安全测试
监控和链路追踪
```

这样回答不会把项目吹过头，也能体现我知道工程落地还需要哪些东西。
