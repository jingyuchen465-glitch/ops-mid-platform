<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bear.mcp.single.core.mapper.McpResourceMapper">
    <sql id="BaseColumns">
        id, resource_uri, name, description, mime_type, object_key, file_name, file_size,
        creator_id, is_enabled AS enabled, publish_status, create_time, update_time
    </sql>

    <select id="findById" resultType="com.bear.mcp.single.core.entity.McpResourceEntity">
        SELECT <include refid="BaseColumns" /> FROM mcp_resource WHERE id = #{id} LIMIT 1
    </select>
    <select id="findByUri" resultType="com.bear.mcp.single.core.entity.McpResourceEntity">
        SELECT <include refid="BaseColumns" /> FROM mcp_resource WHERE resource_uri = #{resourceUri} LIMIT 1
    </select>
    <select id="findEnabledByUri" resultType="com.bear.mcp.single.core.entity.McpResourceEntity">
        SELECT <include refid="BaseColumns" />
        FROM mcp_resource
        WHERE resource_uri = #{resourceUri} AND is_enabled = 1 AND publish_status != 0
        LIMIT 1
    </select>
    <select id="findShareStudioResources" resultType="com.bear.mcp.single.core.entity.McpResourceEntity">
        SELECT <include refid="BaseColumns" />
        FROM mcp_resource
        WHERE creator_id = #{creatorId} OR creator_id IS NULL
        ORDER BY id DESC
    </select>
    <select id="findPublished" resultType="com.bear.mcp.single.core.entity.McpResourceEntity">
        SELECT <include refid="BaseColumns" />
        FROM mcp_resource
        WHERE is_enabled = 1 AND publish_status != 0
        ORDER BY id DESC
    </select>
    <select id="findAll" resultType="com.bear.mcp.single.core.entity.McpResourceEntity">
        SELECT <include refid="BaseColumns" /> FROM mcp_resource ORDER BY id DESC
    </select>
    <insert id="insert" parameterType="com.bear.mcp.single.core.entity.McpResourceEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mcp_resource (
            resource_uri, name, description, mime_type, object_key, file_name, file_size,
            creator_id, is_enabled, publish_status
        ) VALUES (
            #{resourceUri}, #{name}, #{description}, #{mimeType}, #{objectKey}, #{fileName}, #{fileSize},
            #{creatorId}, #{enabled}, #{publishStatus}
        )
    </insert>
    <update id="update" parameterType="com.bear.mcp.single.core.entity.McpResourceEntity">
        UPDATE mcp_resource
        SET resource_uri = #{resourceUri},
            name = #{name},
            description = #{description},
            mime_type = #{mimeType},
            object_key = #{objectKey},
            file_name = #{fileName},
            file_size = #{fileSize},
            creator_id = #{creatorId},
            is_enabled = #{enabled},
            publish_status = #{publishStatus}
        WHERE id = #{id}
    </update>
    <update id="updatePublishStatus">
        UPDATE mcp_resource
        SET is_enabled = #{enabled}, publish_status = #{publishStatus}
        WHERE id = #{id}
    </update>
    <update id="updateEnabled">
        UPDATE mcp_resource
        SET is_enabled = #{enabled}
        WHERE id = #{id}
    </update>
</mapper>
