# Bear MCP Server —— 面试文档

> 本文档基于代码实际实现撰写，所有结论可在仓库中逐行验证。
> 使用方式：第 1、5、6 节必须能脱稿讲；第 12 节必须主动说；第 13 节是防守。

---

## 0. 这个项目到底是什么

一个**企业级 MCP（Model Context Protocol）Server**，核心命题一句话：

> **把 MCP 工具从"编译期的代码"变成"运行期的数据"——并且是在一个不支持这么做的框架（Spring AI）之上做到的。**

普通 MCP Server：工具 = 一个打了 `@Tool` 注解的 Java 方法，改一个工具要改代码、编译、发版、重启。

这个项目：工具 = 数据库里的一行记录（描述 + JSON Schema + Groovy 脚本 + 外部依赖白名单）。新增/修改工具不需要碰代码，配上就能用；并且工具的可见性、可调用性受一套两层权限模型管控，每一次调用都有审计。

再往上一层：**Agent 可以通过 MCP 调用工具，来创建新的工具**——工具是数据，所以这件事在物理上成立。

---

## 1. 30 秒电梯陈述（背下来）

> "我做了一个企业级的 MCP Server。它要解决的问题是：MCP 协议规定工具在 `initialize` 之后由 `tools/list` 声明，而 Spring AI 的实现是启动时扫描 `@Tool` 注解、之后不可变——这跟'工具要能在线配置'的需求是直接冲突的。
>
> 我的做法不是 fork 框架，也不是重写 Server，而是在 **JSON-RPC 协议层**做拦截：MCP over Streamable HTTP 本质就是一个 POST 端点上的 JSON-RPC，我在 Servlet Filter 链里按 `method` 分流，对不同方法做**放行、改写、短路**三种处理。Spring AI 因此退化成一个'帮我执行几个内置方法'的可替换执行器。
>
> 在这个骨架上我做了完整的四原语（Tools / Prompts / Resources / Skills）、两层权限模型、Groovy 动态工具执行、外部 API 与数据源的白名单编排，以及全链路审计。整个系统大概 188 个 Java 文件、18 张表。"

**讲完这段，面试官大概率会追问 Filter 链的细节——那正是你最想聊的地方。**

---

## 2. 速览卡

| 项 | 值 |
|---|---|
| 技术栈 | Spring Boot 3.3.0 / Spring AI 1.1.0 / Java 17 / MyBatis 3.0.3 / MySQL / Groovy 4.0.18 |
| 辅助 | HikariCP、OkHttp 4.12、Hutool 5.8、火山 TOS SDK（对象存储） |
| 前端 | Vue 3.5 + Vite 6 + ant-design-vue 4.2 |
| 规模 | 188 个 Java 文件 / 13.6k 行；18 张表；15 个迁移脚本 |
| 协议 | MCP Streamable HTTP，单端点 `POST /mcp`，端口 8090 |
| 过滤器 | 6 个，`@Order` 10 / 18 / 19 / 20 / 21 / 22 |
| MCP 原语 | Tools、Prompts、Resources（+ 自定义 Skills） |
| 脚本执行 | 固定 8 线程池，单次超时 30s |
| SQL 工具 | 仅 SELECT，最多 500 行，SQL 长度上限 10000 |

---

## 3. 为什么要做：三个真实痛点

**痛点一：工具迭代要发版。**
业务方说"帮我加个查订单的工具"，走一遍改代码 → 提 MR → CI → 发布，最快半天。而工具本身可能只是"调一个已有的 HTTP 接口，把结果整理一下"。**这个改动量和这个流程成本严重不匹配。**

**痛点二：所有人看到同一批工具。**
标准 MCP Server 的 `tools/list` 对所有客户端返回相同结果。但企业里"能查订单"和"能改订单"显然不是同一批人；更进一步，同一个人做数据分析和做运维时，也不该加载同一套工具。

**痛点三：Agent 调用了什么，没人知道。**
工具调用是有副作用的（打接口、查库）。出了问题要能回答：谁、什么时候、用什么参数、调了哪个工具、结果如何。

这三点决定了架构必须包含：**动态化（工具即数据）、权限化（分人分场景）、可观测（全量审计）**。

---

## 4. 架构总览

### 4.1 分层

```
┌──────────────────────────────────────────────────┐
│  MCP 客户端（Cursor / Claude Code / 自研 Agent）  │
└───────────────────────┬──────────────────────────┘
                        │  JSON-RPC over Streamable HTTP
                        ▼
┌──────────────────────────────────────────────────┐
│  网关层  gateway/  —— 6 个 Servlet Filter        │
│  鉴权 · 上下文 · 协议改写 · 动态分发 · 审计       │
└───────────────────────┬──────────────────────────┘
                        │
        ┌───────────────┴────────────────┐
        ▼                                ▼
┌────────────────┐              ┌──────────────────┐
│  Spring AI     │              │  core/ 能力层     │
│  只执行 @Tool  │              │  动态工具 / 脚本  │
│  内置工具      │              │  Prompt/Resource │
└────────────────┘              │  Skill / 审计     │
                                └────────┬─────────┘
                                         ▼
                        ┌────────────────────────────┐
                        │ MySQL(18表) · 外部DB · TOS │
                        │ 外部 HTTP API              │
                        └────────────────────────────┘

旁路：admin/（管理台 API） · share/（创作者+社区） · publicapi/（免登录分发）
```

### 4.2 一次 `tools/call` 的完整时序

```
1. ApiKeyAuthFilter (@Order 10)
   Authorization: Bearer xxx  →  SHA-256  →  查 mcp_user_token
   → 校验 is_active / 未过期 / 用户启用 / 角色启用
   → 聚合角色授权（tool + prompt + resource）
   → 构建 McpUserContext 存入 ThreadLocal
   失败：直接返回 401，Spring AI 永远看不到这个请求

2. McpToolsCallFilter (@Order 21)
   ├─ 缓存 body（CachedRequest，解决 InputStream 只能读一次）
   ├─ 解析 method，非 tools/call 直接放行
   ├─ 统一鉴权 canCall() = 角色授权 ∩ Token 选择
   │  不通过 → 返回 JSON-RPC error + 写审计
   ├─ 是动态工具？
   │  YES → 短路：执行 Groovy，自己拼 JSON-RPC 响应，不调 doFilter
   │  NO  → 放行给 Spring AI，用 ContentCachingResponseWrapper 包住
   │         执行完从响应里判断成功/失败，补一条审计
   └─ finally: ThreadLocal 清理（Tomcat 线程复用，必须清）
```

**这里有个值得强调的点**：内置工具和动态工具走的是**同一个鉴权闸门**（`McpToolsCallFilter:188` 的 `canCall()`）。不存在"内置工具因为是代码写的所以不用管权限"这种漏洞。

---

## 5. 核心设计一：在协议层扩展框架 ⭐️ 最重要

### 5.1 矛盾

Spring AI 的 MCP Server 用 `MethodToolCallbackProvider` 在启动时扫描 `@Tool`，生成一个不可变的工具集合。而项目需求是工具存在数据库里、随时可增删改。

四条路：

| 方案 | 判断 |
|---|---|
| fork Spring AI 改源码 | ❌ 永远无法跟进上游，维护成本失控 |
| 自己从零写 MCP Server | ❌ 重复造轮子，协议细节（会话、SSE、分帧）踩坑成本高 |
| 放弃动态，改成重启加载 | ❌ 产品价值归零 |
| **在 JSON-RPC 协议层拦截** | ✅ |

### 5.2 做法：按 method 分流，三种策略

MCP over Streamable HTTP 的本质是——**一个 POST 端点上跑 JSON-RPC**。既然是开放协议，那框架内部怎么实现根本不重要，我在报文层面处理就行。

| Order | Filter | method | 策略 |
|---|---|---|---|
| 10 | `ApiKeyAuthFilter` | 全部 | 鉴权、建上下文 |
| 18 | `McpResourcesFilter` | `resources/list` `resources/read` | **短路** |
| 19 | `McpInitializeCapabilitiesFilter` | `initialize` | **改写响应** |
| 20 | `McpToolsListFilter` | `tools/list` | **改写响应** |
| 21 | `McpToolsCallFilter` | `tools/call` | **鉴权 + 条件短路** |
| 22 | `McpPromptsFilter` | `prompts/list` `prompts/get` | **短路** |
| — | Spring AI Handler | 其余 | 执行内置 `@Tool` |

- **放行**：内置工具调用，Spring AI 真的执行，Filter 只在外面包一层审计
- **改写**：让 Spring AI 先生成响应，缓冲住，再往里注入/剔除内容
- **短路**：Filter 自己执行、自己拼 JSON-RPC 响应，**不调 `filterChain.doFilter`**

### 5.3 三个必答的实现细节

**① 请求体只能读一次 → `CachedRequest`**

Servlet 的 `InputStream` 读完就没了。但 Filter 必须先读 body 才知道是不是动态工具，读完又可能要放行给 Spring AI 再读一次。所以做了一个 `HttpServletRequestWrapper`，把 body 存成 `byte[]`，每次 `getInputStream()` 都基于 `byte[]` 新建流。（`McpToolsCallFilter:299`）

**② `tools/list` 的改写逻辑**

不是简单拼接。步骤是：
1. 放行给 Spring AI，拿到**完整的**内置工具列表
2. 用 `getUserFinalTools(context)` 算出当前 Token 的最终可见集合
3. **过滤**内置工具（不在集合里的删掉）
4. **追加**数据库里的动态工具，把 `input_schema` 字段反序列化成 MCP 要求的 `inputSchema`

**③ `initialize` 为什么必须改 —— 这是最能体现"读过协议"的点**

MCP 客户端**不会盲目发请求**，它按握手阶段服务端声明的 `capabilities` 决定后续行为。Spring AI 因为自己没实现 Prompts/Resources，在 `initialize` 响应里**不声明这两个 capability**。

结果就是：**你把 `prompts/list` 实现得再完美，客户端根本不会来问。**

所以 `McpInitializeCapabilitiesFilter` 必须回头篡改握手响应，强行注入 `capabilities.prompts` 和 `capabilities.resources`。而且要同时处理两种传输形态——纯 JSON 响应，和 SSE 的 `data: {...}` 分帧。

> **面试话术**：
> "整个项目最难定位的一个 bug 就在这。我的 `prompts/list` 实现完全正确，用 curl 手工发能拿到结果，但 Cursor 里就是一个 Prompt 都不显示。查了很久才意识到问题不在 `prompts/list`，在 `initialize`——客户端是按 capability 声明决定要不要发后续请求的。这件事让我真正理解了 MCP 的握手语义，也是我最后决定在协议层而不是代码层扩展框架的原因。"

### 5.4 这个设计的价值

**Spring AI 被降级成了一个可替换组件。** 项目对它的依赖被压缩到"帮我反射调用几个 `@Tool` 方法"，其余协议行为全部自持。换 SDK、换语言、甚至换传输层，架构都不用动。

---

## 6. 核心设计二：两层权限模型 ⭐️ AI 原生视角

### 6.1 模型

```
第一层：角色授权（能力上限 / ceiling）
  mcp_role_tool      role_code → tool_name
  mcp_role_prompt    role_code → prompt_name
  mcp_role_resource  role_code → resource_uri

第二层：Token 选择（本次装备 / loadout）
  mcp_user_tool_selection      token_id → tool_name
  mcp_user_prompt_selection    token_id → prompt_name
  mcp_user_resource_selection  token_id → resource_uri

有效能力 = 第一层 ∩ 第二层
```

代码里 `tools/list`（可见性）和 `tools/call`（可调用性）用的是同一套判定，不存在"列表里看不见但硬调能调通"的问题。

### 6.2 第二层的真正理由 —— 讲不出这个，这个设计就白做了

面试官一定会问："既然角色已经管住权限了，第二层是不是多余？"

**答案：第二层不是安全设计，是上下文预算管理。**

`tools/list` 的返回值是**要塞进 LLM 上下文的**。工具挂到 100 个会有两个后果：

1. **Token 成本**：每一轮对话都要重复携带全部工具的名称、描述、JSON Schema，几千 token 白烧
2. **选择准确率下降**（更致命）：工具越多，语义相近的工具越多，模型选错的概率越高。10 个工具选对 95%，100 个工具可能掉到 60%

所以设计成：**同一个人，做数据分析用 A Token（加载 3 个查询工具），做运维用 B Token（加载 4 个运维工具）**。角色授权解决"能不能"，Token 选择解决"这次带哪几个"。

> **一句话总结**：`tools/list` 是模型上下文的一部分，所以它需要的是预算管理，不只是访问控制。

### 6.3 Token 安全

- 数据库**不存明文**。鉴权时对请求里的 Bearer Token 做 SHA-256，拿 hash 查 `mcp_user_token.token_hash`（唯一索引）
- 另存一份 AES 加密副本 `token_encrypted`，仅供管理台"重新查看"功能使用
- 列表页只展示 `token_prefix` 掩码
- 记录 `last_used_time` / `last_used_ip`，支持 `expire_time` 和 `is_active` 撤销

---

## 7. 核心设计三：动态工具的执行与隔离

### 7.1 一个动态工具是什么

`mcp_dynamic_tool` 的一行：

| 字段 | 作用 |
|---|---|
| `tool_name` | 工具名，MCP 里的唯一标识 |
| `tool_description` | **给模型看的**，直接决定模型会不会选它 |
| `input_schema` | JSON Schema，`tools/list` 里原样透出 |
| `groovy_script` | 执行逻辑 |
| `linked_request_keys` | 本脚本允许调用的外部 API 白名单 |
| `linked_data_source_ids` | 本脚本允许查询的数据源白名单 |
| `publish_status` | 0 草稿 / 1 上架不公开 / 2 公开 |

### 7.2 为什么是 Groovy

- **同 JVM，零进程开销**，可直接操作 Java 对象，不需要跨语言序列化
- **语法是 Java 超集**，Java 工程师零学习成本
- 有 `SecureASTCustomizer` 这类**编译期**安全钩子，比运行时拦截更早

（诚实地说：Groovy 脚本引擎在国内中台里是成熟套路，这不是本项目的创新点。**创新点在于把它接到 MCP 上，让它成为 Agent 可调用的能力单元。**）

### 7.3 沙箱：双层校验

**第一层 — 执行前字符串预扫描**：拦截明显的危险类名/方法名。

**第二层 — `SecureASTCustomizer`（编译期 AST 检查）**：
- `packageAllowed(false)`：脚本不能声明包
- 静态导入白名单置空
- `receiversBlackList`：禁止对危险类型接收者做调用
- 表达式检查器：检查构造调用和静态调用的类型、方法名

黑名单类：`Runtime` `ProcessBuilder` `System` `Thread` `File` `Files` `Paths` `Socket` `GroovyShell` `GroovyClassLoader`
黑名单方法：`execute` `exit` `halt` `load` `getRuntime` `exec` `start` `forName` `newInstance` `getClassLoader` `getMethod` `getDeclaredMethod` `invoke`

**第三层 — 资源隔离**：固定 8 线程的执行池，单次执行超时 30 秒。

### 7.4 能力白名单：capability-based security

脚本本身**没有**发起任意 IO 的能力。它只能用宿主注入到 Binding 里的几个句柄：

```groovy
params      // 模型传进来的参数
userId      // 调用者身份
userName
toolName
runRequest  // 调外部 API —— 受 linked_request_keys 约束
runSql      // 查外部数据源 —— 受 linked_data_source_ids 约束
```

`runRequest('order_detail', [id: params.orderId])` 里的 `'order_detail'` 如果不在本工具的 `linked_request_keys` 里，直接抛异常。

**这是 capability-based security 的标准形态**：不给权限的通用能力，只给受限的具体句柄。每个工具的"攻击面"由它自己的白名单精确定义。

### 7.5 `runRequest`：企业接口配置化

`mcp_request_config` 把一个后端接口描述成数据：URL、method、headers、`body_template`、`params_default`、连接/读取超时、每分钟限流。执行时做 `{{param}}` 模板插值，用 OkHttp 发出去，返回 `{status, body}`。

**意义**：Agent 接一个新的内部接口，不需要写 Java，配一条记录 + 写几行 Groovy 编排即可。

### 7.6 `runSql`：受控的外部数据源查询

- `mcp_data_source` 存 JDBC 连接信息，密码 AES 加密落库
- 按 `datasource_id` 维度做 Hikari 连接池缓存，避免每次新建连接
- SQL 校验：必须以 `SELECT` 开头、禁止 `;`（阻断多语句）、长度上限 10000
- 结果封顶 500 行，返回 `truncated` 标记告知模型结果被截断

---

## 8. 核心设计四：Agent 自扩展 ⭐️ 野心所在

`core/tools/` 下的一组 Studio 工具，本身也是 MCP 工具，但它们的作用是**创建 MCP 能力**：

| 工具 | 作用 |
|---|---|
| `create_request_config` | 注册一个外部 API 配置 |
| `create_dynamic_tool` | 创建动态工具（草稿态） |
| `update_dynamic_tool_script` | 修改工具脚本 |
| `create_prompt` | 创建 Prompt 模板 |
| `create_resource` | 创建 Resource |
| `create_skill` | 创建 Skill |

于是产生一个闭环：

```
用户："帮我做个查订单的工具，接口是 http://xxx/order/detail"
  ↓
Agent 调 create_request_config   → 注册接口
Agent 调 create_dynamic_tool     → 写 Groovy 编排它
  ↓
管理员发布 / 用户在 Token 里勾选
  ↓
下一轮会话，Agent 从 tools/list 拿到了自己刚造出来的工具
```

**这是自扩展 Agent（self-extending agent）架构。** 绝大多数 MCP 项目停在"人写工具给 Agent 用"，这个项目做到了"Agent 写工具给 Agent 用"。

**它成立的前提，恰恰是第 5 节的"工具即数据"**——如果工具还是编译进 jar 的 `@Tool` 方法，这件事物理上不可能。两个设计在这里闭环。

---

## 9. MCP 四原语全覆盖

市面上 90% 的 MCP Server 只做 Tools。本项目四个都做了，而且 Prompts / Resources 是在 Spring AI **完全不支持**的情况下靠 Filter 从零实现的。

| 原语 | 表 | 说明 |
|---|---|---|
| **Tools** | `mcp_dynamic_tool` | 模型主动调用，有副作用 |
| **Prompts** | `mcp_prompt_template` | 用户主动触发的预设模板，`{{arg}}` 插值，可声明 `linked_tool_names` 提示配套工具 |
| **Resources** | `mcp_resource` | 只读上下文，正文存 TOS 对象存储，按 `resource_uri` 寻址 |
| **Skills** | `mcp_skill` | 自定义原语：一份带 frontmatter 的 `SKILL.md`，打包成 zip 分发 |

**兼容性设计**：部分客户端（如 Cursor）对 MCP Prompts 支持不完整。为此额外提供了内置工具 `render_prompt`——**把 Prompt 能力降级成一个 Tool 暴露出去**，让不支持 `prompts/get` 的客户端也能用。这个 workaround 值得讲，它说明你在处理真实的客户端生态碎片化。

**分发的最后一公里**：Skill 支持 `install.sh` 一键安装、Cursor deeplink 一键接入、免登录 zip 下载。造出来的东西要能让人真的装上。

---

## 10. Text-to-SQL：语义层比 Prompt 重要

`docs/text-to-sql-resources/` 下 67 个 Markdown，分四层：

| 目录 | 内容 |
|---|---|
| `yanque-schema/` | **46 张表**的字段说明、含义、关联关系、安全标记 |
| `yanque-business-terms/` | 业务术语：对象、状态机、枚举含义（7 个领域） |
| `yanque-metrics/` | 指标口径：计算公式、主表、时间字段、可用维度（7 个领域） |
| `yanque-agent/` | 一个 Skill + 一个 Prompt，编排 `list_data_sources` / `query_data_source` |

**这套分层背后的认知**：

> Text-to-SQL 的瓶颈从来不是 Prompt 写得好不好，是**语义层建得全不全**。
>
> "上个月退款率"——"退款率"在哪张表？分母是订单数还是金额？"上个月"按下单时间还是退款时间？这些都不是模型能猜出来的，是**必须喂给它的业务知识**。

所以这三层对应三类知识：**schema 层**（数据长什么样）、**术语层**（业务词怎么映射到字段）、**指标层**（口径怎么算）。这是数据资产建设问题，不是 AI 问题。

（当前状态：这套语料是待上传进 `mcp_resource` / `mcp_prompt_template` 的素材，还没有接进代码，面试时如实说。）

---

## 11. 数据模型（18 张表）

```
【身份与权限】
mcp_user                     管理台用户，BCrypt 密码
mcp_role                     角色
mcp_user_role                用户 ↔ 角色
mcp_role_tool                角色 → 工具授权     ┐
mcp_role_prompt              角色 → Prompt 授权   ├ 第一层：能力上限
mcp_role_resource            角色 → Resource 授权 ┘
mcp_user_token               MCP Token（SHA-256 hash 唯一索引）
mcp_user_tool_selection      Token → 加载哪些工具     ┐
mcp_user_prompt_selection    Token → 加载哪些 Prompt   ├ 第二层：本次装备
mcp_user_resource_selection  Token → 加载哪些 Resource ┘

【能力资产】
mcp_dynamic_tool             动态工具（脚本 + 双白名单）
mcp_prompt_template          Prompt 模板
mcp_resource                 Resource（正文在 TOS）
mcp_skill                    Skill（SKILL.md 在 TOS）
mcp_request_config           外部 API 配置（runRequest 的目标）
mcp_data_source              外部数据源（runSql 的目标，密码 AES）

【运营】
mcp_audit_log                全量调用审计
mcp_community_like           社区点赞
```

**几个设计点**：
- 四类资产统一用 `publish_status` 三态：0 草稿 / 1 上架不公开 / 2 公开到社区
- 迁移脚本 15 个，全部 `IF NOT EXISTS` / `INSERT IGNORE`，可重复执行
- `spring.sql.init.mode: never`，启动不重建表——审计日志必须跨重启保留

---

## 12. 已知短板与改进路线 ⭐️ 主动说，别等着被问

**这一节是加分项，不是减分项。** 面试官一定会挖洞，你先把洞列出来，性质就从"这人不知道自己代码有问题"变成"这人清楚知道离生产还差什么"。

### P0 — 上生产前必须修

| 问题 | 现状 | 修复方案 |
|---|---|---|
| **配置里有明文凭据** | `application.yml` 里 RDS 密码、JWT secret 明文，且已提交 git | 全部改环境变量 / 配置中心，轮换密钥，清理 git 历史 |
| **认证 = 管理员，没有授权层** | JWT 拦截器只验签名不查角色，任何登录用户能调 `/api/admin/**` 建用户改权限 | 引入接口级角色校验；`/api/share/**` 限制为只能操作自己的资源 |
| **动态工具没有归属字段** | `mcp_dynamic_tool` 无 `creator_id`，`update_dynamic_tool_script` 可改任意工具脚本 | 加 `creator_id` + 全链路 owner 校验（Prompt/Resource/Skill 已有，Tool 遗漏了） |

### P1 — 架构性缺陷

| 问题 | 分析 | 方案 |
|---|---|---|
| **Groovy 脚本无编译缓存** | 每次调用 new 一个 `GroovyShell` 重新编译 → ClassLoader 泄漏 + 每次付编译开销 | 按 `脚本内容 hash` 做 `Script` 缓存，脚本变更时失效 |
| **超时不取消线程** | `future.get(timeout)` 抛异常后从不 `cancel(true)`，脚本线程继续跑；8 个死循环打满线程池 | `cancel(true)` + 脚本内插桩中断点；线程池加隔离舱和拒绝策略 |
| **沙箱是黑名单** | 黑名单漏了 `Eval`（用默认配置编译嵌套脚本可逃逸）；检查器只看方法调用，`.class` / `.classLoader` 这类属性访问完全不检查 | 改白名单：`setIndirectImportCheckEnabled(true)` + import 白名单 + 属性访问检查；长期看应迁到独立进程 / GraalVM 隔离 |
| **`runRequest` 无 SSRF 防护** | URL 无 scheme / 内网 IP 校验，`{{param}}` 可插进 URL → 可打云元数据 `169.254.169.254` | 出站域名白名单 + 内网网段拦截 + 模板值转义 |
| **API 配置无归属校验** | 可以把别人的私有 API 配置绑进自己的工具，借服务器重放对方存储的 Authorization 头（混淆代理人） | 绑定时校验 creator 或"已公开" |
| **`runSql` 防护偏薄** | 只拦 `;` 和要求 `SELECT` 开头，未拦 `INTO OUTFILE`（DB 主机任意写文件）、`LOAD_FILE()` | 改用 SQL 解析器做白名单校验；`setReadOnly(true)` + `setMaxRows` + `setQueryTimeout`（当前 500 行是客户端截断，库那边已经算完了） |

### P2 — 工程质量

- **请求体全量 INFO 日志**：header 和最多 20KB body 明文落盘，第三方 Authorization、SQL、Prompt 参数全泄露 → 降级 DEBUG + 脱敏
- **测试基本为零**：4 个测试文件 704 行，其中 652 行是 Groovy/Optional 语言练习，不 import 任何项目代码。鉴权、RBAC 过滤、白名单强制、动态工具分发**零覆盖** → 优先补这四条链路的 `@SpringBootTest`
- **每请求 ~7 次 SELECT + 1 次写**（更新 `last_used_time`）→ Token 上下文加本地缓存 + 异步写
- **限流计数器 `static` 且永不淘汰**，按 `key:分钟` 无限增长 → 换 Caffeine 带 TTL，或上 Redis 做分布式限流
- **审计同步写库**在请求线程上 → 异步队列 + 批量落库
- **AES 用 Hutool 默认 ECB 无 IV**，且密钥由 JWT 签名密钥派生（密钥复用）→ 改 GCM + 独立密钥
- **错误码永远 HTTP 200**，401 也是 200 + body 里带 code → 破坏标准工具链
- **前端 `App.vue` 5314 行**装了管理台/社区/Studio 三个产品 → 拆路由拆组件

---

## 13. 高频追问 Q&A

### 关于 MCP 协议

**Q：MCP 和 Function Calling 是什么关系？**
Function Calling 是**模型能力**——模型能输出结构化的调用意图。MCP 是**协议标准**——规定工具怎么被发现（`tools/list`）、怎么被调用（`tools/call`）、传输层怎么走。类比：Function Calling 是"能打电话"，MCP 是"电话号码簿 + 通话协议"。没有 MCP，每接一个 Agent 客户端都要重写一遍适配。

**Q：为什么选 Streamable HTTP 不选 stdio？**
stdio 是本地单进程模型，一个客户端一个 Server 进程，无法做集中式的权限和审计。Streamable HTTP 是服务化的：一个 Server 服务多个客户端，Token 区分身份，这才撑得起"企业级"。

**Q：`initialize` 返回的 `Mcp-Session-Id` 有什么用？**
标识一次会话，后续请求要带回来。项目里也把它记进了审计上下文，用于把一次会话内的多次调用串起来。

### 关于架构

**Q：在 Filter 里改响应，SSE 流式怎么办？**
`initialize` 和 `tools/list` 这两个改写场景是**一问一答**的，不是长流，所以可以安全缓冲后改写。实现上做了兼容：如果响应是 `data: {...}` 的 SSE 分帧，解析出 JSON 改完再按原格式写回。**（诚实补充：`prompts/*` 和 `resources/*` 的短路目前统一返回纯 JSON，如果客户端强制协商 `text/event-stream` 会有问题——这是已知待修项。）**

**Q：`McpUserContext` 用 ThreadLocal，异步/响应式场景会不会串？**
当前是 WebMVC 同步模型，Tomcat 一请求一线程，`finally` 里清理即可。**但有个已知问题**：Groovy 脚本在独立线程池执行，ThreadLocal **不会**传过去。目前脚本需要的身份信息是显式作为 Binding 变量传进去的，所以功能上没问题；但如果以后脚本内部要调用依赖上下文的服务就会拿到 null。正解是改成显式的上下文对象参数传递，而不是依赖 ThreadLocal。

**Q：为什么不直接用 Dify / Coze？**
它们是完整的 Agent 平台，包含对话编排、知识库、工作流。这个项目**只做 MCP Server 这一层**——不管对话怎么编排，只负责"把企业能力标准化地暴露给任意 MCP 客户端"。定位是**能力供给侧**，客户端可以是 Cursor、Claude Code、也可以是自研 Agent。两者是互补不是替代。

### 关于安全

**Q：你的 Groovy 沙箱，我三行代码能逃逸，你信吗？**
**信。** 我的沙箱是黑名单，黑名单本质上是防不住有心攻击者的。已知至少两个洞：`Eval.me()` 会用默认的无安全配置的 `CompilerConfiguration` 编译嵌套脚本；AST 检查器只检查 `MethodCallExpression`，`.class` / `.classLoader` 这类属性访问路径完全没覆盖。

我当前的定位是"防止业务同学误操作"，不是"防止恶意攻击者"。真要做到后者，正确的路径是三步：**改白名单（import 白名单 + 属性访问检查）→ 独立进程隔离 + seccomp → 容器化沙箱**。这是我下一步要做的。

**Q：Token 泄漏了怎么办？**
`is_active` 置 0 立即失效；有 `expire_time` 强制过期；有 `last_used_ip` 可追溯。**当前缺的是**：泄漏窗口内的行为回溯要靠翻审计表，没有主动告警。应该加异常调用检测（IP 突变、调用量突增）。

**Q：动态工具是让用户上传代码执行，这不就是 RCE？**
本质上是，所以设计上是**多层纵深防御**：创建工具需要授权（不是谁都能调 `create_dynamic_tool`）→ 创建出来是草稿态（`publish_status=0`）需要发布 → 脚本执行有沙箱 → 外呼受双白名单约束 → 有超时和线程隔离 → 全量审计。

**但我要诚实**：当前 `update_dynamic_tool_script` 缺归属校验，这一层纵深有个缺口，是我列在 P0 的第一批要修的。

### 关于性能

**Q：每次请求都查 7 次库，压力大吗？**
现在是教学/内部规模，没问题。生产化的优化路径很清楚：Token 上下文按 token_hash 做本地缓存（Caffeine，TTL 1 分钟）+ 变更时主动失效；`last_used_time` 改异步批量写；`tools/list` 的 N+1 改成一次批量查。这些都是确定性优化，不需要架构改动。

**Q：如果有 10 万个工具怎么办？**
`tools/list` 全量返回在 10 万规模下**协议层面就不成立**——不是性能问题，是模型上下文根本装不下。正确解法是**分层发现**：先按领域返回一个粗粒度的工具目录，让模型选定领域后再展开具体工具；或者做工具的语义检索，按用户 query 动态返回 Top-K。这已经超出 MCP 当前协议的表达能力了，需要在应用层做。

### 压迫式追问（准备好被这么问）

**Q：这不就是个 Groovy 规则引擎套了个 MCP 的壳？**
Groovy 引擎确实不是新东西，我也不会说它是。**真正的工作量和判断在别处**：一是在 Spring AI 这个"工具集合启动即固化"的框架上做出动态化，靠的是协议层拦截而不是改框架；二是权限模型的两层设计，尤其第二层是为了 LLM 的上下文预算而不是安全；三是让 Agent 能反过来创建工具。**Groovy 只是执行器，换成 JS 引擎架构一行不用改。**

**Q：改 `initialize` 的响应算不算 hack？协议升级了怎么办？**
是 hack，我承认。但这是权衡后的选择：替代方案是 fork Spring AI，那样每次上游升级都要重新 merge，成本更高。我的做法把 hack 收敛在一个 177 行的 Filter 里，边界清晰。**如果 Spring AI 后续原生支持 Prompts/Resources，我只需要删掉这个 Filter**，其他代码一行不动。这比改在框架内部可逆得多。

**Q：18 张表的 CRUD，有什么技术含量？**
CRUD 那部分确实没有，那是工作量不是技术。有含量的是三处：协议层扩展的判断、两层权限背后的上下文预算认知、以及"工具即数据"如何让 Agent 自扩展成为可能。**如果只让我讲 30 秒，我会只讲第一个。**

---

## 14. 可能的白板题

**① 画出一次 `tools/call` 的完整链路** → 见 4.2

**② 如果要支持工具的灰度发布怎么设计？**
`publish_status` 已有三态基础。灰度需要加：`mcp_dynamic_tool_version` 表存多版本脚本 + 一张灰度规则表（按 user_id / role / 百分比）。`tools/list` 时按规则决定返回哪个版本，`tools/call` 时带上版本号确保一次会话内一致。审计表加 `tool_version` 字段以便对比两个版本的成功率和耗时。

**③ 怎么保证脚本执行不影响主服务？**
现状：独立线程池（8 线程）+ 30 秒超时。缺陷：超时不取消，池会被占死。
完整方案：① `cancel(true)` + 脚本编译期插入中断检查点 ② 按租户/工具做线程池隔离舱，单个工具打满不影响其他 ③ 加熔断，工具连续超时自动下线 ④ 长期看应该独立进程，通过 IPC 通信，进程崩溃不影响主服务。

**④ 审计日志怎么支持"查某次 Agent 会话都调了什么"？**
现在按 user + tool 记录。要支持会话维度需要把 `Mcp-Session-Id` 落进审计表并加索引；再进一步可以加 `trace_id` 串联"一次用户提问 → N 次工具调用"，这时候应该接标准的 OpenTelemetry 而不是自己造。

---

## 15. 速记卡（面试前 5 分钟看）

**三个必讲的点，按优先级**：
1. **协议层扩展** —— 不 fork 框架，在 JSON-RPC 层做放行/改写/短路；`initialize` capability 注入是最能证明"读过协议"的细节
2. **两层权限** —— 角色授权管安全，Token 选择管**上下文预算**（tools/list 要进模型上下文）
3. **Agent 自扩展** —— 工具即数据，所以 Agent 能造工具给 Agent 用

**必须主动说的短板**：沙箱是黑名单（`Eval` 可逃逸）、动态工具缺归属校验、缺授权层、脚本无编译缓存、超时不取消、测试覆盖近乎为零。

**关键数字**：6 个 Filter（10/18/19/20/21/22）· 18 张表 · 4 个 MCP 原语 · 8 线程池 / 30s 超时 · SQL 500 行上限 · 188 文件 13.6k 行 · 端口 8090

**最容易翻车的三个问题**：
- "沙箱能不能绕过" → **承认能**，说清三步改进路径
- "这不就是 Groovy 引擎" → 把价值拉回**协议层判断 + 上下文预算 + 自扩展**
- "10 万工具怎么办" → 说清这是**协议层面不成立**，需要分层发现或语义检索

**一句话收尾**：
> "这个项目让我真正想清楚了一件事——给 Agent 做工具平台，最稀缺的资源不是算力也不是存储，是**模型的上下文**。所有的设计取舍最后都会回到这一点。"
